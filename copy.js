/* copy.js — every string on the site, by id.

   Empty on purpose: with no entries here, each page shows the copy written in
   its own markup. Open any page with ?edit=1, type on the page, then click
   "Download copy.js" and replace this file. From then on these values win.

   Strings may contain merge tokens — {first_name}, {her_name}, {goal},
   {goal_lower}, {hardest_part}, {issue_1}, {outcome_1}, {promo_code} — which
   are filled from the visitor's answers.

   Loaded before funnel.js on every page. */
window.COPY = {
};
